const Ui =  (props) => {
    const {messages} = props;
    return (
        {
            messages.filter((message,index)=>{
                <>
                    
                </>
            })
        }
    )
}

export default Ui;